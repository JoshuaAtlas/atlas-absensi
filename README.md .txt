# Supabase Configuration
# Salin dari Project Settings → API Keys

VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_PUBLIC_KEY

# Untuk local development: buat file .env.local dan copy values di atas
# Untuk production (Vercel): set di Vercel Dashboard → Project Settings → Environment Variables